extends ../layout/index

block content
    div(class="py-10")
        h1(class="text-4xl my-10 font-extrabold text-center") Free
            span(class="font-normal") Bazar
        h2(class="text-center text-2xl font-extrabold")= page 

        p(class="text-cl font-bold text-center my-10")= msg
        div(class="text-center")
            p go back to 
                a(href="/auth/login" class="font-bold text-blue-700") Login.